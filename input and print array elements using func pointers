#include <stdio.h>
void array(int *p,int n )
{
	

	for (int i = 0; i < n; i++)
	{
		printf("%d  ", *(p + i));
	}
	printf("\n");
}
int main()
{
    int arr[5] = {10, 20, 30, 40, 50};
    	int *p = arr;
    void (*fptr)(int *,int)=array;
    fptr(p,5);
    return 0;
}
