#include<stdio.h>
#include<string.h>
int main()
{
    char *ptr[]={"red","blue","Yellow"};
    //int len=strlen(*ptr);
    int len=sizeof(ptr)/sizeof(ptr[0]);
    int i;
    for(int i=0;i<len;i++)
    {
        printf(" %s ",ptr[i]);
    }
    return 0;
}
